# WHETSTONE Research Digest: In-Repo Archaeology (Doctrine-Loop Baseline)

**Commissioned**: 2026-08-14 by the session lead for CONOP WHETSTONE
**Author**: read-only exploration agent (archaeology lane)
**Status**: evidence record, verbatim as delivered; corrections land as dated addenda, never edits
**Baseline**: committed state HEAD `01d3073`, branch `topic/kb-graph-traversal`, plus two then-uncommitted Wave 0 artifacts

**Addendum (2026-08-14, same day)**: §3's "Layer-5/Layer-6 escalation decision, ~15 months nominal" is wrong; the task landed 2026-05-19 (commit `028ed67`), making it 87 days old at survey time. Caught by `docs/reviews/20260814_conop_whetstone_debate.md` Critical 1. The blocked-on-nonexistent-data finding stands.

---

## 1. Loop instances (field experience → doctrine change)

Ordered by date. Latency = observation → doctrine artifact changed.

- **L1 — 2026-03-17, clone cleanup exposes playbook gaps.** `docs/sessions/20260317_rmi_reboot_clone_cleanup.md` (lines 29-32): cleaning rmi-reboot from the template produced two hub memory entries, incl. the guardrail "never strip `.claude/commands/`". Artifact changed: `docs/design/from_template_to_project.md` (clone-cleanup steps), `tests/conftest.py` dead fixtures. **Latency: 14 days** (closed `docs/tasks.md:54`, 2026-03-31). The two memory files no longer exist — `.claude/agent-memory/` is gitignored (`.gitignore:56`), so that half of the feedback was lost.
- **L2 — 2026-03-25, magic-movies CRLF churn.** 948-insert/948-delete phantom diff, pure line-ending churn (`docs/sessions/20260325_gitattributes_and_doctrine_push.md:15`; narrated in `docs/doctrine-updates.md:359-372`). Artifact: `.gitattributes` shipped in the hub 2026-03-25 but **recorded as doctrine only 2026-06-28** (`docs/doctrine-updates.md` 2026-06-28 entry; commit `6d28740`). **Latency: ~3 months from artifact to doctrine entry**; the entry states the reason plainly — it was never recorded, so "most downstream repos never adopted it."
- **L3 — 2026-03-30, unread-notification collision.** Five downstream repos held unread 2026-03-24 notifications; the script would have overwritten them (`docs/sessions/20260330_doctrine_propagation_append.md:17-31`). Artifacts: append mode in `scripts/propagate_doctrine.py`, and the rule codified in `docs/propagation-protocol.md:88-96`. **Latency: same session.**
- **L4 — 2026-04-21, downstream convention out-ran the hub.** A downstream repo (velocity-scoring) had drifted to a cleaner agent-output convention than the template encoded; the hub adopted the downstream's design rather than inventing one (`docs/sessions/20260421_docs_reviews_convention_and_python311.md:12,18,70`). Artifacts: 4 agent files, `.claude/README.md`, `config/project.yaml`, `docs/doctrine-updates.md` 2026-04-21 entry. **Latency: same session, propagated same day to 11 repos.**
- **L5 — 2026-05-19, review escalates to an enforcement layer.** Pass 4 code-reviewer: "nothing in the repo deterministically enforces TDD" (`docs/reviews/20260519_pass4_doctrine_audit.md`; synthesis in `docs/sessions/20260519_enforcement_layer_and_propagation_prep.md:40-67`). Artifacts: `.claude/hooks/post-tool-shift-left-audit.sh`, `.claude/skills/shift-left-testing/ENFORCEMENT.md`, `.claude/settings.json`, CLAUDE.md principle. **Latency: same day.** Layer-6 hard block rejected by MAUT (`docs/reviews/20260519_pass4_enforcement_maut.md`).
- **L6 — 2026-05-19, Pass 5 catches a blocker in the entry itself.** Proposer reproduced an invalid bash `case` alternation in the draft doctrine entry §7 (`docs/reviews/20260519_pass5_grill.md`; `docs/sessions/20260519_enforcement_layer_and_propagation_prep.md:77`). Artifact: the 2026-05-19 entry, fixed **pre-propagation, same day**. Same shape as the uv field exercise, but sourced from review rather than field.
- **L7 — 2026-05-24, MEGAN CONOP-002 §4.2 retrospective.** Six permanent `dev-*` branches retired downstream after they forced resync ceremony for no review benefit (`.claude/skills/using-topic-branches/SKILL.md:96-100`). Artifacts: new skill + CLAUDE.md "Branching" principle + `docs/doctrine-updates.md` 2026-06-28 entry (commit `e9eb51c`). **Latency: ~5 weeks.**
- **L8 — 2026-05-28, heimdall-darkroom, first Windows downstream.** Three artifacts shipped by the 2026-05-19 cycle failed on Windows (`docs/sessions/20260529_windows_portability_fixes.md:29-37`). Artifacts: `scripts/adopt_doctrine.py`, `.claude/hooks/post-tool-shift-left-audit.sh`, `src/myproject/utils/logger.py`, `pyproject.toml`, 2 tests, `docs/doctrine-updates.md` 2026-05-29 entry. **Latency: 1 day for the two new bugs; 37 days for the `tzdata`/`ZoneInfoNotFoundError` bug** (hit 2026-04-22, patched locally, "port back to utils" carry-over rotted across two session-ends). The session states the lesson explicitly (`:312`): "The act of writing a fix and the act of upstreaming a fix are different tasks."
- **L9 — 2026-07-17, four-repo planning retrospective.** 20 churn episodes across magic-movies/swimming-analytics/elephant-graveyard/tactics-game (`docs/reviews/20260717_planning_retrospective_*.md`). Artifacts: `docs/plans/CONOP-FORMAT.md`, `docs/plans/OPORD-FORMAT.md` hardened (commit `bbc6cb4`), `.claude/commands/task.md` wiring. **Latency: same day for 8 format changes; 2 findings still parked at 28 days** (`docs/tasks.md:16`, see §3).
- **L10 — 2026-07-19/20, prose skill field-tested on its own corpus.** Applying the new skill to the hub surfaced ~150 violations across 31 files and a rule-8 scope question that changed the rule (`docs/sessions/20260720_prose_doctrine_hypothesis_relicense.md:36-53`; `docs/reviews/20260719_writing_style_sweep.md`). Artifacts: whole-corpus sweep, `CLAUDE.md` kernel, `LANGUAGE.md` cruft list with a term-of-art carve-out, ADR-0001/0002 **Amendments** sections recording a blessed append-only exception. **Latency: same cycle.**
- **L11 — 2026-07-20, hypothesis strategy design finds a live crash.** `exponential()` ZeroDivisionError for `|rate| < ~2.2e-16` (same session doc, :54-57). Artifacts: Known-Issue block in `docs/doctrine-updates.md:341-348` + `docs/tasks.md:11`. **Latency: doctrine warning same day; fix still open at 25 days.**
- **L12 — 2026-07-26, veil-engine bootstrap exposes Day-1 playbook drift.** `docs/sessions/20260726_veil_engine_bootstrap.md:23`. The trap: Step 4's "delete `docs/plans/*.md`" now deletes the CONOP/OPORD format specs. Artifacts changed **so far**: `docs/propagation-protocol.md:171` roster line only. The playbook fix is **still open** (`docs/tasks.md:14`, P2, amended 2026-08-03 to also require uv steps). **Latency: 19 days and counting.**
- **L13 — 2026-08-03, uv field exercise corrects the entry before propagation.** `docs/sessions/20260803_uv_environment_doctrine.md:22`. Three corrections shipped into the entry and `SETUP.md`: discover venvs by `pyvenv.cfg` not by name (name-glob saw 5 of 10), `uv venv --clear` over `rm -rf`, freeze-as-insurance. Artifacts: `docs/doctrine-updates.md:49-107`, `.claude/skills/python-venv-management/` 3.0.0. **Latency: same day, pre-propagation.**
- **L13b — 2026-08-03, parity gate finds sibling spec bugs.** Three repos' specs were provably wrong: elephant-graveyard missing `scikit-learn` (48 tests silently uncollected), quest-engine unbounded `mcp>=1.0`, dnd-minis editable install that had never worked (`docs/tasks.md:24`; session doc :25). Doctrine artifact changed: the "three dependency-drift classes" paragraph in `docs/doctrine-updates.md:73-79`. **Latency: same day**; fixes committed in each sibling repo, **not pushed** (still local).
- **L14 — 2026-08-13→14, kb-graph review kills an unfalsifiable trigger (in flight).** `docs/reviews/20260813_kb_graph_adoption_review.md` C1: the proposal's skill-first bet "cannot lose" — no observable, no metric, and `.claude/audits/` does not exist to back the claim. Artifacts now in the working tree: `.claude/skills/traversing-the-knowledge-base/SKILL.md` v1.0.0 (five-session numeric criterion) and `.claude/commands/pcc.md` **check 5, reference integrity**. **Latency: ~1 day, uncommitted.**
- **Non-instance worth recording:** 2026-05-19 Pass 6 found the Day-1 playbook predated the whole doctrine cycle (11 FAILs, `docs/reviews/20260519_pass6_day1_playbook_audit.md`); rewritten same day 569→791 lines. The same document drifted again 14 months later (L12) — the drift recurs because nothing gates it.

## 2. Feedback-channel inventory

| # | Channel | Direction | Automation | Verifies downstream integration? |
|---|---|---|---|---|
| 1 | `scripts/propagate_doctrine.py` → `.claude/upstream-update.md` | **Push** | Automated write, manual trigger | **No.** Writes and exits (`scripts/propagate_doctrine.py:83-112`); no read-back, no receipt, no state. |
| 2 | Session-start Step 3.6 | **Pull** (downstream) | Manual, model-dependent | No. `.claude/commands/session-start.md:33-38`: surface, never delete; the file's deletion is the only "consumed" signal and nothing reads it. |
| 3 | `scripts/adopt_doctrine.py` | **Pull** (downstream runs it) | Semi-automated: 13 mechanical steps + 12-item manual checklist (`scripts/adopt_doctrine.py:296`) | No. Idempotent skip-don't-overwrite; refuses CUSTOMIZE artifacts; reports what *it* changed, never what the maintainer did. |
| 4 | Session-end workflow | **Pull** (hub-side) | Manual, model-dependent | No. `.claude/commands/session-end.md` Steps 4/4.5/5 write `docs/tasks.md`, `config/project.yaml`, `docs/sessions/`. This is the only mechanism that parks a lesson, and it has no expiry or closure check. |
| 5 | PostToolUse audit hook (Layer 4) | **Push** (evidence) | Deterministic, automated | Detects missing test partners only. **Dead in practice: `.claude/audits/` does not exist in this working tree**, so the `>20 MISSING_TEST/month` escalation trigger (`.claude/skills/shift-left-testing/ENFORCEMENT.md:81-85`) has never had data. Log is gitignored (`.gitignore:58`) → never aggregated across machines or repos. |
| 6 | Agent review reports `docs/reviews/` | **Pull** | Manual (commissioned per session) | No. 21 reports; no finding-status tracking. Findings close only if a human copies them into `docs/tasks.md`. |
| 7 | Cross-repo retrospectives | **Pull** | Manual, one-off | No. `docs/reviews/20260717_planning_retrospective_*.md`: read-only agents commissioned from the hub against 4 downstream repos. Never repeated. |
| 8 | Parity gates (test-count parity on rebuild) | **Pull** | Manual, ad-hoc | Verifies the *environment*, not doctrine adoption. Only run once (2026-08-03); the strongest evidence-producing channel in the corpus and it has no schedule. |
| 9 | Cross-repo citation in session docs | **Pull** | Manual | No. `docs/sessions/20260529_windows_portability_fixes.md:10` cites heimdall's session by URL. Documented failure mode at `:312` (4-month rot). |
| 10 | `.claude/agent-memory/` | Neither (local sink) | Automated write | No, and **it cannot propagate**: gitignored, per-machine. The contradiction is recorded only inside the sink itself (`.claude/agent-memory/proposer/agent_memory_scope_gitignored.md`). Currently holds two live corrections nobody else can see: fleet size is 19 not 15, and skills need an evidence layer. |
| 11 | `docs/tasks.md` parking lot | Pull | Manual | The de facto closure mechanism for every other channel. No aging, no owner field, no review cadence. |
| 12 | `CHANGELOG.md` | One-way record | Manual | No. |
| 13 | `/pcc` check 5, reference integrity (**uncommitted at survey time**) | Pull | Deterministic grep, manual trigger | New; verifies *hub* link integrity, not downstream adoption. |
| 14 | Roster in `docs/propagation-protocol.md:167-173` | Informational | None | Explicitly not read by the script (`:173`). Discovery is filesystem-only; no opt-out exists (`:82`). |

**Bottom line for the CONOP:** every channel from field to doctrine is pull + manual. The only push channel is #1, and the only stated verification is an unscheduled human watch — "Watch for `.claude/upstream-update.md` deletions or session docs in downstream repos" (`docs/sessions/20260519_adopt_doctrine_helper_and_propagation.md:107`), never recorded as performed in any later session doc.

## 3. Dead or dormant lessons

- **Four doctrine entries appear never to have been shipped by the script.** The script extracts only `entries[0]` (`scripts/propagate_doctrine.py:47-66`; rule stated at `docs/propagation-protocol.md:44`). Recorded propagation runs (`docs/tasks.md:34,36,44,46,59` + sessions) shipped the 03-24, 03-26, 04-21, 05-19, 07-20, 08-03 entries. No run shipped **2026-03-31 (session-start git sync)**, **2026-05-29 (Windows patch)**, **2026-06-28 (.gitattributes)**, or **2026-06-28 (branching doctrine)** — each was overtaken as `entries[0]` before the next run. Precedent shows the team knows this hazard (commit `d996d10`, "Merge Cycle 8 into the unpropagated Cycle 7 entry") but the 05-29 and 06-28 entries were never folded in.
- **Parked retrospective findings, 28 days open** (`docs/tasks.md:16`): (1) cross-component lessons-learned register — elephant-graveyard re-learned the master-clock fix on a structural twin a week later (`docs/reviews/20260717_planning_retrospective_elephant_graveyard.md:64-72`); (2) inherit sibling-repo pillars at project inception (config-authority existed in 4 of 6 sister repos first). Marked "candidates for a future doctrine cycle" with no owner.
- **Day-1 playbook drift, 19 days open** (`docs/tasks.md:14`, from L12). The destructive trap (delete `docs/plans/*.md` → deletes the format specs) is live in `docs/design/from_template_to_project.md` Step 4 (line ~111) right now. Also missing from it: prose-skill adoption, `using-topic-branches`, `.gitattributes`, Apache-2.0 LICENSE, hypothesis plumbing, the consumer-side propagation contract, and a `coverage_threshold` 80-vs-50 mismatch.
- **Layer-5/Layer-6 escalation decision, permanently blocked** (`docs/tasks.md:13`; carried in Next Steps of both 2026-05-19 sessions). It is gated on "1-2 sessions of audit-log data" and **the audit log does not exist**. Independently rediscovered by an agent on 2026-08-13 (`.claude/agent-memory/code-reviewer/skills-need-evidence-layer.md`). *(Age corrected to 87 days; see Addendum.)*
- **CONTEXT.md is stale by three propagation cycles.** `CONTEXT.md:112` says Last Updated 2026-05-19; `:34-35` still claim "most recent doctrine propagation: 5th cycle (2026-04-21); 11 repos." Actual: 2026-08-03, 15 repos. The uv cycle patched only its hard-rules line (`:50`) — `docs/doctrine-updates.md:118` confirms the entry touched CONTEXT.md for exactly one line. A dedicated skill exists for this file and did not fire.
- **Pass 2 audit findings never applied** (`docs/reviews/20260519_pass2_audit.md:138-141`, 2.C): the civilian/military crosswalk is duplicated in three places and the `docs/propagation-protocol.md:141-149` copy is still the short 7-row version missing `backbrief` and `METL`, still inlined rather than pointing at LANGUAGE.md. Recommendation "pick one canonical home" never executed.
- **Opt-out mechanism**: named as an open question in `docs/propagation-protocol.md:84` (2026-05-19), escalated by a concrete case 2026-07-26 (elephant-graveyard's show is over but it keeps receiving cycles), still unresolved (`docs/tasks.md:15`).
- **`exponential()` tiny-rate crash**, open 25 days, shipped downstream as a Known Issue rather than a fix (`docs/tasks.md:11`; `docs/doctrine-updates.md:341-348`).
- **Three sibling spec-bug commits are local-only** (`docs/tasks.md:24`, elephant-graveyard `383ebc7`, quest-engine `aa9304c`, dnd-minis `3f9d4a4`) — "not pushed" as of 11 days ago. Field fixes sitting in the same un-upstreamed state that L8 identified as the repo's classic failure mode.
- **Two 2026-06-28 doctrine entries have no session doc.** `docs/sessions/` jumps 2026-05-29 → 2026-07-20; commits `6d28740` and `e9eb51c` shipped doctrine with no session record, so their field triggers survive only as one-line provenance notes.
- **No 2026-07-17 session doc** either: the four-repo retrospective, ADR-0002, the rename, and the CONOP/OPORD hardening (commits `acfba0c`, `b826949`, `bbc6cb4`, `d996d10`) are recorded only in reviews, `docs/tasks.md:83-85`, and the doctrine entry.
- **Fleet-size drift**: decision documents use 15 repos; the roster lists 19 (`docs/propagation-protocol.md:171`). Recorded only in gitignored agent memory (`.claude/agent-memory/code-reviewer/verify-load-bearing-counts.md`).
- **Five March decision-science docs**: untracked since March, verified absent from this tree 2026-08-13; recoverable only from another machine's clone or never (`docs/tasks.md:8`).
- **Never-propagated protocol footer drift**: `docs/propagation-protocol.md:177` says Last Updated 2026-07-17 while its roster line says "as of 2026-07-26."

## 4. Corpus census (committed state, HEAD `01d3073`)

- **Session docs: 18** (`docs/sessions/*.md`), 2026-03-12 → 2026-08-03. Gaps: no June docs, no 2026-07-17 doc, no doc yet for the 2026-08-13/14 kb-graph work.
- **Skills: 8 committed directories, 32 files** under `.claude/skills/` (31 skill files + `SKILLS_FRAMEWORK.md`). Sidecar counts: shift-left-testing 13, configuration-management 6, writing-simple-and-direct 5, python-venv-management 3, four single-file skills at 1.
  - **Revised at least once: 3 of 8.** `python-venv-management` 3.0.0, `shift-left-testing` 2.1.0, `configuration-management` 2.0.0.
  - **Still at 1.0.0: 5** — maintaining-project-context, maintaining-ubiquitous-language, recording-architecture-decisions, using-topic-branches, writing-simple-and-direct.
  - **+1 untracked at survey time**: `traversing-the-knowledge-base` v1.0.0.
- **ADRs: 2 accepted + 1 format spec.** Both accepted ADRs carry a 2026-07-20 **Amendments** section (the blessed append-only exception).
- **Doctrine entries: 10** (`grep -c '^## 2026' docs/doctrine-updates.md`), 1,231 lines, 2026-03-24 → 2026-08-03. The 2026-07-20 entry is three independently-adoptable Parts. Largest single entry: 2026-05-19, 23 artifacts, ~500 lines.
- **Propagation cycles actually run: 8** — 03-24 (4 repos), 03-25 (9), 03-26 (9), 03-30/31 (10: 5 new/5 append), 04-21 (11: 3/8), 05-19 (12: 5/7), 07-20 (14: 7/7), 08-03 (15: 6/9). Trend: monotonically rising repo count, and roughly half of every recent cycle lands as an *append* onto unread debt.
- **Review reports: 21** (`docs/reviews/*.md`) — 13 from the single 2026-05-19 cycle, 4 retrospectives (2026-07-17), 1 style sweep (2026-07-19), 3 kb-graph (2026-08-13).
- **Plan docs: 4** (`docs/plans/`) — 2 format specs, 1 CONOP (`decision_science_utility.md`, status "Proposed — awaiting code-reviewer challenge", stale since March), 1 proposal (kb-graph, Draft). **Zero plans have reached a terminal status** despite the terminal-status rule the formats mandate.
- **Design docs: 3**, including the 791-line Day-1 playbook.
- **Other**: 2 scripts, 1 hook, 6 commands, 5 agents, 4 team templates, 3 agent-memory stores (gitignored, all written 2026-08-13).
